#include <stdio.h>

int main(){
    int x , y;
    int result = x + y;
    printf("Enter the value of x :\n",x );
    scanf("%d", &x);
    printf("Enter the value of x :\n",y );
    scanf("%d", &y);
    printf("The required sum is :%d\n", result = x + y);
    scanf("%d", &sum);
    return 0;
}
